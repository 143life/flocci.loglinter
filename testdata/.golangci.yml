version: "2"

linters:
  default: none
  enable:
    - flocciloglint
  settings:
    custom:
      flocciloglint:
        type: "module"
        description: "Log message linter"
        settings:
          use_regex: true
          sensitive_patterns:
            - "password"
            - "token"
            - "secret"
            - "api_key"
          check_first_lowercase: true
          forbid_emoji: true
          forbid_special_chars: true
          allow_only_ascii: true